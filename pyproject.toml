[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "aiidalab-react-qe-backend"
version = "0.1.0"
description = "FastAPI backend for AiiDAlab React QE app"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic>=2.0"]
